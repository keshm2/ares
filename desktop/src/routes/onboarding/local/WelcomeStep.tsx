export function WelcomeStep() {
  return (
    <p>
      This quick setup walks through your environment, coding agent, profile details, resumes,
      and notifications — everything aplyx needs to start searching and applying on your
      behalf. You can change any of it later in Settings, and every step saves as you go, so it&rsquo;s
      safe to close and come back.
    </p>
  );
}
